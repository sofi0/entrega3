{{>header}}
{{mensaje}}


<h3>pagina de estudiantes</h3>

<form method="post" action="/eliminar">
		<!-- <div class="row">
			<div class="col-3">
		    <label for="staticEmail2">ID Curso</label>
		    <input type="text" class="form-control" id="idCurso" value="" name="idCur" size="5">
		    <button type="submit" class="btn btn-dark">Mostrar</button>
			</div>
	    </div>-->				

	    <div class="form-group">
                <label for="state_id" class="control-label">curso</label>
                    <select class="form-control" name="idCurso">
                            {{#each cur}}
                                <option value='{{this._id}}'>{{this.nombre}}</option>
                            {{/each}}
                    </select>
            <button type="submit" class="btn btn-dark">Mostrar</button>     
        </div>


	    <div class="container-md">
			<input name="idCursoEliminar" type="hidden" value="{{this._id}}">
				<table class="table">
				  <thead class="thead-dark">
				    <tr>
				      <th scope="col">Curso</th>
				      <th scope="col">Cedula</th>
				      <th scope="col">Telefono</th>
				      <th scope="col">Nombre</th>
				      <th scope="col">Email</th>
				      <th scope="col">Modalidad</th>
				      <th scope="col"></th>
				    </tr>
				  </thead>
				  <tbody>

				  	{{#each estudiante}}
       				<tr>
       					<td>{{this.idCursos}}</td>
       					<td>{{this.ced}}</td>
       					<td>{{this.tel}}</td>
				        <td>{{this.nomb}}</td>
				        <td>{{this.email}}</td>
				        <td><button type="submit" class="form-control btn btn-primary btn-sm" name="nomb" value="{{this._idCursos}}">eliminar</button></td>
      				</tr>
      				{{/each}}
					
					<!-- {{listar estudiante}} -->
				    
				  </tbody>
				</table>
		</div>
		<br>
</form>

{{>footer}}